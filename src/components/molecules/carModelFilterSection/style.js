import { StyleSheet } from "react-native";
export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
        zIndex: -1
    },
    cardsHolder: {
        // flex: 1
        width: "100%",
    }
})